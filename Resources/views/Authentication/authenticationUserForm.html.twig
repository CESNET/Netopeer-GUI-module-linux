{% form_theme formConfigAuthentication 'FITModuleLinuxBundle:Form:linuxFormTheme.html.twig' %}
{% for user in formConfigAuthentication.user %}
    <div class="user-subform {{ 'user-' ~ user.vars.value.name}}">
        {{ form_row(user.name) }}


            {{ form_row(user.password) }}


        <button type="button" class="change-password-button">change password</button>
        <div class="change-password-form" style="display: none">
            {{ form_row(user.oldPassword) }}
            {{ form_row(user.newPassword) }}
        </div>

        {# TODO: #}
        {# <div class="subform-list authorized-key">
            {% for authorizedKey in formConfigAuthenticationUser.authorizedKey %}
                {{ form_row(authorizedKey.keyNameValue) }}
                {{ form_row(authorizedKey.keyNameXpath) }}

                {{ form_row(authorizedKey.algorithmValue) }}
                {{ form_row(authorizedKey.algorithmXpath) }}

                {{ form_row(authorizedKey.keyDataValue) }}
                {{ form_row(authorizedKey.keyDataXpath) }}
            {% endfor %}
        </div> #}
    </div>
{% endfor %}